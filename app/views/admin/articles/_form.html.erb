<%= simple_form_for([:admin, @article]) do |f| %>
  <%= f.input :title %>
  <%= f.input :body, as: 'text' %>
  <%= f.input :tag_names %>
  <%= f.input :published %>
  <%= f.input :image, as: 'file' %>
  <%= f.button :submit%>
<% end %>
